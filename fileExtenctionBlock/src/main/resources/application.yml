spring:
  application:
    name: file-extension-block
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

app:
  git:
    sha: ${GIT_SHA:dev}

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8089,http://fileext.yeseulkim.cloud}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /docs
