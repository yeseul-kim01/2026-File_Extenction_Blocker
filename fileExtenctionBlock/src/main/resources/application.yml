spring:
  application:
    name: file-extension-block
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

app:
  git:
    sha: ${GIT_SHA:dev}

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8089}
